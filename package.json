{
  "name": "astrometrica-map",
  "version": "1.0.0",
  "description": "3D map visualization for AstroMetrica with duplicate checking functionality",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check-duplicates": "node duplicate-checker.js",
    "check-duplicates-strict": "node duplicate-checker.js --threshold 2500",
    "check-duplicates-loose": "node duplicate-checker.js --threshold 10000"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "three": "^0.171.0",
    "three-spritetext": "^1.9.3"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
